'use client';

import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#4352E5',
    },
    secondary: {
      main: '#E7E9FC',
    },
  },
});

export default theme;